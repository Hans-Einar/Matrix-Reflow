cmake_minimum_required(VERSION 3.20)
project(MatrixReflowLinux VERSION 0.1.0 LANGUAGES C CXX)
set(CMAKE_C_STANDARD 99)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_C_EXTENSIONS OFF)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

find_package(PkgConfig REQUIRED)
pkg_check_modules(X11 REQUIRED IMPORTED_TARGET x11)
pkg_check_modules(EPOXY REQUIRED IMPORTED_TARGET epoxy)
find_package(Freetype REQUIRED)
pkg_check_modules(GLIB REQUIRED IMPORTED_TARGET glib-2.0>=2.66)

file(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/generated")
function(embed_resource symbol input)
    set(output "${CMAKE_CURRENT_BINARY_DIR}/generated/${symbol}.h")
    add_custom_command(OUTPUT "${output}"
        COMMAND ${CMAKE_COMMAND} "-DINPUT=${input}" "-DOUTPUT=${output}"
            "-DSYMBOL=${symbol}" -P "${CMAKE_CURRENT_SOURCE_DIR}/cmake/EmbedResource.cmake"
        DEPENDS "${input}" "${CMAKE_CURRENT_SOURCE_DIR}/cmake/EmbedResource.cmake"
        VERBATIM)
    set(${symbol}_HEADER "${output}" PARENT_SCOPE)
endfunction()
embed_resource(matrix_font "${CMAKE_CURRENT_SOURCE_DIR}/../windows/Matrix-Code.ttf")
add_library(reflow-atlas STATIC src/font_atlas.cpp ${matrix_font_HEADER})
target_include_directories(reflow-atlas PUBLIC src PRIVATE "${CMAKE_CURRENT_BINARY_DIR}/generated")
target_link_libraries(reflow-atlas PRIVATE Freetype::Freetype)
target_compile_options(reflow-atlas PRIVATE -Wall -Wextra -Wpedantic)

embed_resource(fullscreen_vertex "${CMAKE_CURRENT_SOURCE_DIR}/shaders/fullscreen.vert")
embed_resource(control_fragment "${CMAKE_CURRENT_SOURCE_DIR}/shaders/control.frag")
embed_resource(composite_fragment "${CMAKE_CURRENT_SOURCE_DIR}/shaders/composite.frag")
embed_resource(glyph_vertex "${CMAKE_CURRENT_SOURCE_DIR}/shaders/glyph.vert")
embed_resource(glyph_fragment "${CMAKE_CURRENT_SOURCE_DIR}/shaders/glyph.frag")
embed_resource(bloom_threshold_fragment "${CMAKE_CURRENT_SOURCE_DIR}/shaders/bloom_threshold.frag")
embed_resource(bloom_downsample_fragment "${CMAKE_CURRENT_SOURCE_DIR}/shaders/bloom_downsample.frag")
embed_resource(bloom_upsample_fragment "${CMAKE_CURRENT_SOURCE_DIR}/shaders/bloom_upsample.frag")
embed_resource(crt_fragment "${CMAKE_CURRENT_SOURCE_DIR}/shaders/crt.frag")
add_library(reflow-gl STATIC src/crt.cpp ${crt_fragment_HEADER} ${bloom_upsample_fragment_HEADER} src/bloom.cpp ${bloom_threshold_fragment_HEADER} ${bloom_downsample_fragment_HEADER} src/x11_host.cpp src/glx_context.cpp src/gl_resources.cpp src/renderer.cpp
    src/glyph_lab.cpp src/simulation.cpp ${glyph_vertex_HEADER} ${glyph_fragment_HEADER}
    ${fullscreen_vertex_HEADER} ${control_fragment_HEADER} ${composite_fragment_HEADER})
target_include_directories(reflow-gl PUBLIC src PRIVATE "${CMAKE_CURRENT_BINARY_DIR}/generated")
target_link_libraries(reflow-gl PUBLIC PkgConfig::X11 PkgConfig::EPOXY reflow-atlas mmcore)
target_compile_options(reflow-gl PRIVATE -Wall -Wextra -Wpedantic)

add_library(mmcore STATIC ../core/mmcore.c)
target_include_directories(mmcore PUBLIC ../core)
target_link_libraries(mmcore PUBLIC m)
target_compile_options(mmcore PRIVATE -Wall -Wextra -Wpedantic)

add_library(reflow-settings STATIC src/settings.cpp)
target_include_directories(reflow-settings PUBLIC src)
target_link_libraries(reflow-settings PUBLIC mmcore PkgConfig::GLIB)
target_compile_options(reflow-settings PRIVATE -Wall -Wextra -Wpedantic)

add_executable(matrix-reflow src/main.cpp)
target_link_libraries(matrix-reflow PRIVATE mmcore reflow-atlas reflow-gl reflow-settings)
target_compile_options(matrix-reflow PRIVATE -Wall -Wextra -Wpedantic)

include(CTest)
if(BUILD_TESTING)
    find_package(Python3 REQUIRED COMPONENTS Interpreter)
    add_test(NAME registration-test COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/registration_test.py)
    add_test(NAME settings-xml-test COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/settings_xml_test.py $<TARGET_FILE:matrix-reflow> ${CMAKE_CURRENT_SOURCE_DIR}/xscreensaver/matrix-reflow.xml)
    add_executable(settings-test tests/settings_test.cpp)
    target_link_libraries(settings-test PRIVATE reflow-settings)
    add_test(NAME settings-test COMMAND settings-test)
    add_test(NAME settings-cli-test COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/settings_cli_test.py $<TARGET_FILE:matrix-reflow>)
    add_executable(core-smoke tests/core_smoke.cpp)
    target_link_libraries(core-smoke PRIVATE mmcore)
    target_compile_options(core-smoke PRIVATE -Wall -Wextra -Wpedantic)
    add_test(NAME core-smoke COMMAND core-smoke)
    add_executable(atlas-test tests/atlas_test.cpp)
    target_link_libraries(atlas-test PRIVATE reflow-atlas mmcore)
    add_test(NAME atlas-test COMMAND atlas-test)
    add_executable(simulation-test tests/simulation_test.cpp)
    target_link_libraries(simulation-test PRIVATE reflow-gl)
    add_test(NAME simulation-test COMMAND simulation-test)
    add_executable(allocation-test tests/allocation_test.cpp)
    target_link_libraries(allocation-test PRIVATE mmcore)
    target_link_options(allocation-test PRIVATE -Wl,--wrap=malloc -Wl,--wrap=calloc)
    add_test(NAME allocation-test COMMAND allocation-test)
    add_executable(graphics-test tests/graphics_test.cpp)
    target_link_libraries(graphics-test PRIVATE reflow-gl)
    add_executable(glyph-render-test tests/glyph_render_test.cpp)
    target_link_libraries(glyph-render-test PRIVATE reflow-gl)
    add_executable(borrowed-window-test tests/borrowed_window_test.cpp)
    target_link_libraries(borrowed-window-test PRIVATE reflow-gl)
    add_executable(bloom-benchmark tests/bloom_benchmark.cpp)
    target_link_libraries(bloom-benchmark PRIVATE reflow-gl)
    add_executable(crt-filter-test tests/crt_filter_test.cpp)
    target_link_libraries(crt-filter-test PRIVATE reflow-gl)
    add_executable(crt-lifecycle-test tests/crt_lifecycle_test.cpp)
    target_link_libraries(crt-lifecycle-test PRIVATE reflow-gl)
    add_executable(bloom-host-test tests/bloom_host_test.cpp)
    target_link_libraries(bloom-host-test PRIVATE reflow-gl)
    add_executable(bloom-test tests/bloom_test.cpp)
    target_link_libraries(bloom-test PRIVATE reflow-gl)
    option(REFLOW_GL_TESTS "Run GLX tests on the current DISPLAY with CTest" OFF)
    if(REFLOW_GL_TESTS)
        add_test(NAME crt-filter-test COMMAND crt-filter-test)
        add_test(NAME crt-lifecycle-test COMMAND crt-lifecycle-test)
        add_test(NAME bloom-host-test COMMAND bloom-host-test)
        add_test(NAME bloom-test COMMAND bloom-test)
        add_test(NAME graphics-test COMMAND graphics-test)
        add_test(NAME glyph-render-test COMMAND glyph-render-test)
        add_test(NAME borrowed-window-test COMMAND borrowed-window-test)
        add_test(NAME cli-lifecycle-test COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tests/cli_lifecycle_test.py $<TARGET_FILE:matrix-reflow>)
        set_tests_properties(crt-filter-test crt-lifecycle-test bloom-host-test bloom-test graphics-test glyph-render-test borrowed-window-test cli-lifecycle-test PROPERTIES LABELS graphics TIMEOUT 30)
    endif()
endif()

include(GNUInstallDirs)
install(TARGETS matrix-reflow RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
set(REFLOW_XSCREENSAVER_HACK_DIR "${CMAKE_INSTALL_LIBEXECDIR}/xscreensaver" CACHE STRING "XScreenSaver effects directory")
set(REFLOW_XSCREENSAVER_CONFIG_DIR "${CMAKE_INSTALL_DATADIR}/xscreensaver/config" CACHE STRING "XScreenSaver XML directory")
install(TARGETS matrix-reflow RUNTIME DESTINATION ${REFLOW_XSCREENSAVER_HACK_DIR})
install(FILES xscreensaver/matrix-reflow.xml DESTINATION ${REFLOW_XSCREENSAVER_CONFIG_DIR})
